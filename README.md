Backend proxy API for BudgetStream clients.

These services are node Azure Functions and are intended to be configured as the backend api for the Static Web App frontend. See the `budgetstream` source repo.